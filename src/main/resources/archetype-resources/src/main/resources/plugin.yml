name: ${artifactId}
version: ${version}  
main: ${package}.${artifactId}
# All above are required, all below here are optional.
description: Place description here...
load: STARTUP
author: 
authors: []
website: http://nowhere.com
database: false
depend: []
prefix: ${artifactId}
softdepend: []
loadbefore: []
commands:
permissions:
# API-Version only applies to 1.13 or 1.14 this disables backwards compatiblity measures pre-1.13 do not include api-version: tag
api-version:
        